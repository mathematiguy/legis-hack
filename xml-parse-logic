XML Parsing logic for legislation

<act>
    <cover>ignore everything</cover>
    <contents>ignore</contents>
    <front>ignore</front>
    <body>
        get everything but ignore content between tags
            <cf>
            <notes>
        
        Then:
            <label> or <label-para> = line break + indent
            </label> = whitespace
            <def-term> = line break + bold
            </def-term> = whitespace
            <crosshead> = line break
            <eqn-line> = line break + indent
            </eqn-line> = line break 
            <variable-def> = line break
            </variable-def> = white space
    </body>
    <schedule.group>ignore everything inside this</schedule.group>
    <end>ignore everything inside this</end>
</act>
